#include <stdio.h>
#include <stdlib.h>

int main(){
int len;
scanf("%d", &len);
int a[len];
int i;
for(i=0; i<len; i++)
	scanf("%d", &a[i]);
insertionsor(a,len);
for(i=0; i<len; i++)
	printf("%d\n", a[i]);
return 0;
}

void insertionsort ( int a[], int len){
int j, i;
int key;
for(j=2; j<len; j++){
		key=a[j];
		i=j-1;
		while (i>0 && a[i]>key){
				a[i+1]=a[i]
				i=i-1;
				}
			a[i+1]=key;
			}
}
